export interface BaseComponentProps {
  /**
   * Optionally enables logging lifecycle events
   * and debug messages to the console.
   *
   * This value must be disabled for production
   * builds.
   */
  debug?: boolean;
}
